a streamlit app to calculate the engineer's estimate

to set up a url for the app:
ssh -R 80:localhost:[PORT] localhost.run

this will expose the app running on [PORT] to the internet via a public URL
is this secure? not really. but we have a password!